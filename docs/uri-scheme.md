# URI Scheme

PVTP may use a custom scheme such as:

```text
pvtp://images/cat-001
```

A custom scheme usually needs:
- an installed app,
- browser integration,
- or an SDK that knows how to handle it.

The protocol can still be implemented over standard HTTPS transport underneath.
